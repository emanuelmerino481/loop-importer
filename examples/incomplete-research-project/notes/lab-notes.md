# Lab notes

- The seed-42 run may have used a newer split than seed 7.
- “Best” currently means the highest number seen in a JSON file, not a verified comparison.
- GPU utilization was not recorded; elapsed time alone is insufficient for GPU-hours accounting.
- Need a human decision on the scientific unit, primary metric, baseline, and failure policy.
