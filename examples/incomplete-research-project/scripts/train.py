"""Unfinished training entrypoint retained as import evidence."""

DEFAULT_SEED = 42


def train(config_path: str) -> None:
    raise NotImplementedError(f"Training is intentionally disabled; config={config_path}")
